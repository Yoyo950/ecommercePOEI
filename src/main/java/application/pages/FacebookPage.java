package application.pages;

import application.utils.WebDriverManager;

public class FacebookPage extends BasePage{
    public FacebookPage(WebDriverManager webDriverManager) {
        super(webDriverManager);
    }
}
