package application.pages;

import application.utils.WebDriverManager;

public class TwitterPage extends BasePage{
    public TwitterPage(WebDriverManager webDriverManager) {
        super(webDriverManager);
    }
}
